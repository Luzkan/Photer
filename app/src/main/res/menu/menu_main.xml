<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context="com.luzkan.photer.MainActivity">

    <item
        android:id="@+id/action_menudrop"
        android:orderInCategory="100"
        android:icon="@drawable/ic_menu"
        app:showAsAction="ifRoom"
        android:title="@string/Menu">
        <menu>
            <item
                android:id="@+id/menu_columns"
                android:orderInCategory="200"
                android:title="@string/columns"
                android:icon="@drawable/ic_columns">
                <menu>
                    <item android:id="@+id/action_increase"
                          android:title="@string/increase"
                          android:orderInCategory="210"
                          app:showAsAction="never"/>

                    <item android:id="@+id/action_decrease"
                        android:title="@string/decrease"
                        android:orderInCategory="220"
                        app:showAsAction="never"/>
                </menu>
            </item>
            <item
                android:id="@+id/action_add"
                android:orderInCategory="300"
                android:title="@string/add"
                android:icon="@drawable/ic_add">
            </item>
        </menu>
    </item>
</menu>
